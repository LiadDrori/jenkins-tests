kind: Pod
spec:
  imagePullSecrets:
    - name: regcred
  containers:
  - name: dude-on-shipod
    image: node:16.13
    command:
    - sleep
    args:
    - "99d"
  securityContext:
    prevailed: true